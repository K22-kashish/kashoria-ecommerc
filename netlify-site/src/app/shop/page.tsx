'use client';import Link from 'next/link';import {useMemo,useState} from 'react';import {categories,products} from '@/lib/products';
export default function Shop(){const [q,setQ]=useState('');const [cat,setCat]=useState('All');const [sort,setSort]=useState('featured');const list=useMemo(()=>products.filter(p=>(cat==='All'||p.category===cat)&&p.name.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>sort==='low'?a.price-b.price:sort==='high'?b.price-a.price:a.name.localeCompare(b.name)),[q,cat,sort]);return <main className="section"><div className="container"><div className="section-head"><div><div className="eyebrow">Shop</div><h1 style={{fontSize:52}}>Crochet collection</h1></div><span className="muted">{list.length} products</span></div><div className="filters"><input className="input" placeholder="Search products…" value={q} onChange={e=>setQ(e.target.value)}/><select className="select" value={cat} onChange={e=>setCat(e.target.value)}><option>All</option>{categories.map(c=><option key={c}>{c}</option>)}</select><select className="select" value={sort} onChange={e=>setSort(e.target.value)}><option value="featured">Featured</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option><option value="name">Name</option></select></div><div className="grid">{list.map(p=><Link className="card" href={`/product/${p.id}`} key={p.id}><img src={p.image} alt={p.name}/><div className="card-body"><div className="muted">{p.category}</div><h3>{p.name}</h3><div className="price">₹{p.price}</div><span className="btn alt">View product</span></div></Link>)}</div></div></main>}
