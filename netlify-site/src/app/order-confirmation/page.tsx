'use client';import Link from 'next/link';import {useSearchParams} from 'next/navigation';export default function Confirm(){const q=useSearchParams();return <main className="section"><div className="container box" style={{textAlign:'center',maxWidth:650}}><div className="eyebrow">Thank you</div><h1 style={{fontSize:60}}>Order received ♡</h1><p>Your order ID is <strong>{q.get('order')||'—'}</strong>.</p><p className="muted">Keep this ID and your phone number to track your order.</p><div className="actions" style={{justifyContent:'center'}}><Link className="btn" href="/track">Track order</Link><Link className="btn alt" href="/shop">Continue shopping</Link></div></div></main>}
