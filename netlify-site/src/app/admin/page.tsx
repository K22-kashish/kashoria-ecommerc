'use client';import {useEffect,useState} from 'react';export default function Admin(){const [orders,setOrders]=useState<any[]>([]);useEffect(()=>{fetch('/api/orders?admin=1').then(r=>r.json()).then(x=>setOrders(x.orders||[]))},[]);const revenue=orders.filter(o=>o.payment_status==='paid').reduce((s,o)=>s+Number(o.total||0),0);return <><h1>Dashboard</h1><div className="stats"><div className="stat"><div className="muted">Revenue</div><h2>₹{revenue}</h2></div><div className="stat"><div className="muted">Orders</div><h2>{orders.length}</h2></div><div className="stat"><div className="muted">Paid</div><h2>{orders.filter(o=>o.payment_status==='paid').length}</h2></div><div className="stat"><div className="muted">Pending</div><h2>{orders.filter(o=>o.payment_status!=='paid').length}</h2></div></div><div className="box" style={{marginTop:20}}><h2>Recent orders</h2><table className="table"><thead><tr><th>ID</th><th>Customer</th><th>Total</th><th>Status</th></tr></thead><tbody>{orders.slice(0,10).map(o=><tr key={o.id}><td>{o.id}</td><td>{o.customer?.name||'—'}</td><td>₹{o.total}</td><td>{o.order_status}</td></tr>)}</tbody></table></div></>}
