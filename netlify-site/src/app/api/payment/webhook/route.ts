import {NextResponse} from 'next/server';import crypto from 'crypto';import {supabase} from '@/lib/server';export async function POST(req:Request){const raw=await req.text();const sig=req.headers.get('x-razorpay-signature')||'';const secret=process.env.RAZORPAY_WEBHOOK_SECRET;if(secret){const expected=crypto.createHmac('sha256',secret).update(raw).digest('hex');if(expected!==sig)return NextResponse.json({error:'Invalid webhook'},{status:400})}const e=JSON.parse(raw);const p=e?.payload?.payment?.entity;if(p&&supabase){await supabase.from('orders').update({payment_status:'paid',razorpay_payment_id:p.id}).eq('razorpay_order_id',p.order_id)}return NextResponse.json({ok:true})}
